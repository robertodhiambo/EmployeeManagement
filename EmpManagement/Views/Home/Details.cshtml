@model EmpManagement.Models.Employee

@{
    ViewData [ "Title" ] = "Employee Details";
}

@{
    var employee = ViewData [ "Employee" ] as EmpManagement.Models.Employee;
}

<div class=" row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
               
            </div>
            <div class="card-body text-center">
                <img class="card-img-top" src="~/image/image1.jpg" />
                <h4>Employee ID : @Model.Id</h4>
                <h4>Employee EMAIL : @Model.Email</h4>
                <h4>Employee DEPARTMENT : @Model.Department</h4>
            </div>
            <div class="card-footer text-center">
           <a class="btn btn-primary" asp-controller="home" asp-action="index">Back</a>   
             @*<a class="btn btn-primary" asp-action="edit" asp-controller="employee" asp-route-id="@Model.Id">Edit</a>*@   

                <a href="#" class="btn btn-primary">Delete</a>
            </div>
        </div>
    </div>
</div>
